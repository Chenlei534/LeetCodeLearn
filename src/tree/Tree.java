package tree;

public class Tree {

    public TreeNode createTree() {
        TreeNode head = null;

        return head;
    }

    public TreeNode addTreeNode(TreeNode head, TreeNode node, int data, boolean isRight) {

        if (head == null) {

        }

        return head;
    }
}
